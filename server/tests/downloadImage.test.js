describe('Server tests', () => {
  it('Search user in database - Should return user', () => {
    expect(true).toEqual(true);
  });
  it('Function for checking the validity of received data during authorization - Should return true', () => {
    expect(true).toEqual(true);
  });
  it('Function of checking the validity of the received data during registration - Should return true', () => {
    expect(true).toEqual(true);
  });
  it('The function of getting the list of files by the current user - Should return file list', () => {
    expect(true).toEqual(true);
  });
  it('The function of getting the list of emails by the current user - Should return emails list', () => {
    expect(true).toEqual(true);
  });
  it('The function of getting the list of phoneNumbers by the current user - Should return phoneNumbers list', () => {
    expect(true).toEqual(true);
  });
  it('Function of checking the operation of a neural network by the input vector - Should return drawing type', () => {
    expect(true).toEqual(true);
  });
  it('Checking the function to delete a file from the database - Should return new file list', () => {
    expect(true).toEqual(true);
  });
  it('Checking the function of saving the file to the database - Should return new file list', () => {
    expect(true).toEqual(true);
  });
  it('The function of checking the receipt of the gravatar link by user - Should return true', () => {
    expect(true).toEqual(true);
  });
  it('The function of creating a new gravatar - Should return true', () => {
    expect(true).toEqual(true);
  });
  it('Photo change function - after successful development of the function should return the true', () => {
    expect(true).toEqual(true);
  });
  it('User data update check function - Should return new user information', () => {
    expect(true).toEqual(true);
  });
  it('The function of getting the organization by the current user - Should return info by organization', () => {
    expect(true).toEqual(true);
  });
});
